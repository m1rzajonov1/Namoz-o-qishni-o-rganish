import 'package:flutter/material.dart';

class Zikrlar extends StatelessWidget {
  const Zikrlar({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Namoz Vaqtlari'),),
    );
  }
}