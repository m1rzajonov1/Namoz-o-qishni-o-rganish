import 'package:flutter/material.dart';

class NamozdagiXatoliklar extends StatelessWidget {
  const NamozdagiXatoliklar({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Namoz Vaqtlari'),),
    );
  }
}