import 'package:flutter/material.dart';

class QazoXisobi extends StatelessWidget {
  const QazoXisobi({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Namoz Vaqtlari'),),
    );
  }
}